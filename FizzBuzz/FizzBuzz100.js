/* Repeat it until you can't think of anything but FizzBuzz */

// Concat

const fizzBuzz = (n) => {
    let result = [];

    for (let i = 1; i <= 0; i++) {
        let arr = "";

        if (i % 3 === 0) arr += "Fizz";
        if (i % 5 === 0) arr += "Buzz";
        if (arr === "") arr += i;
            result.push(arr);
    }
    return result;
}

var fizzBruzz = function(n) {
    let arr = []
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                arr.push("FizzBuzz")
            else if (i % 3 === 0)
                arr.push("Fizz")
            else if (i % 5 === 0)
                arr.push("Buzz")
            else
                arr.push(i + "")
        }
    return arr;
}

const fizzBUzz = (n) => {
    let arr = [];
        for (let i = 1; i <= n; i++) {
            let string = "";

            if (i % 3 === 0) string += "Fizz";
            if (i % 5 === 0) string += "Buzz";
            if (string === "") string += i;
                arr.push(string);
        };
    return arr;
}

var fizzBUZz = function(n) {
    let arr = [];
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                arr.push("FizzBuzz")
            else if (i % 3 === 0)
                arr.push("Fizz")
            else if (i % 5 === 0)
                arr.push("Buzz")
            else
                arr.push(i + "")
        }
    return arr;
};

var fizzBuZz = function(n) {
    let arr = []
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                arr.push("FizzBuzz")
            else if (i % 3 === 0)
                arr.push("Fizz")
            else if (i % 5 === 0)
                arr.push("Buzz")
            else
                arr.push(i + "")
        }
    return arr;
}

const fiZZbUzZ = (n) => {
    let result = [];
        for (let i = 1; i <= n; i++) {
            let arr = "";

            if (i % 3 === 0) arr += "Fizz";
            if (i % 5 === 0) arr += "Buzz";
            if (arr === "") arr += i;
                result.push(arr);
        }
    return result;
};

const fRiZZbUzZ = (n) => {
    let result = [];
        for (let i = 1; i <= n; i++) {
            let arr = "";
            
            if (i % 3 === 0) arr += "Fizz";
            if (i % 5 === 0) arr += "Buzz";
            if (arr === "") arr += i;
                result.push(arr);
        }
    return result;
}

const friZZZbuZz = (n) => {
    let stringResult = []
        for (let i = 1; i <= n; i++) {
            let arr = "";

            if (i % 3 === 0) arr += "Fizz";
            if (i % 5 === 0) arr += "Buzz";
            if (arr === "") arr += i;
                stringResult.push(arr);
        }
    return stringResult;
};

var frAzzBuszz = function(n) {
    let arr = [];
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                arr.push("FizzBuzz")
            else if (i % 3 === 0)
                arr.push("Fizz")
            else if (i % 5 === 0)
                arr.push("Buzz")
            else
                arr.push(i + "")
        }
    return arr;
}

var fB = function(n) {
    let string = [];
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                string.push("FizzBuzz")
            else if (i % 3 === 0)
                string.push("Fizz")
            else if (i % 5 === 0)
                string.push("Buzz")
            else
                string.push(i + "")
        }
    return string;
}

const fBuzz = (n) => {
    let bizzle = [];
        for (let i = 1; i <= n; i++) {
            let fizzle = "";

            if (i % 3 === 0) fizzle += "Fizz";
            if (i % 5 === 0) fizzle += "Buzz";
            if (fizzle === "") fizzle += i;
                bizzle.push(fizzle);
        }
    return bizzle;
}

const fBrizzle = (n) => {
    let result = [];
        for (let i = 1; i <= n; i++) {
            let stringResult = "";

            if (i % 3 === 0) stringResult += "Fizz";
            if (i % 5 === 0) stringResult += "Buzz";
            if (stringResult === "") stringResult += i;
                result.push(stringResult);
        }
    return result;
}

var fBizz = function(n) {
    let result = [];
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                result.push("FizzBuzz")
            else if (i % 3 === 0)
                result.push("Fizz")
            else if (i % 5 === 0)
                result.push("Buzz")
            else
                result.push(i + "")
        }
    return result;
}

const fbuizz = (n) => {
    let result = []
        for(let i = 1; i <= n; i++) {
            let arr = "";

            if (i % 3 === 0) arr += "Fizz";
            if (i % 5 === 0) arr += "Buzz";
            if (arr === "") arr += i;
                result.push(arr)
        }
    return result;
}

var bfuzz = function(n) {
    let result = [];
        for (let i = 1; i <= n; i++) {
            if (i % 3 === 0 && i % 5 === 0)
                result.push("FizzBuzz")
            else if (i % 3 === 0)
                result.push("Fizz")
            else if (i % 5 === 0)
                result.push("Buzz")
            else
                result.push(i + "")
        }
    return result;
}

const fbuss = (n) => {
    let result = [];
        for (let i = 1; i <= n; i++) {
            let arr = "";

            if (i % 3 === 0) arr += "Fizz";
            if (i % 5 === 0) arr += "Buzz";
            if (arr === "") arr += i;
                result.push(arr);
        }
    return result;
}